By default, all available plugins are enabled and loaded from the plugins
# directory at startup (specified via general:plugins_folder).
plugins: {
	disable = "libjanus_nosip.so,libjanus_videoroom.so,libjanus_streaming.so,libjanus_sip.so,libjanus_lua.so,libjanus_voicemail.so,libjanus_recordplay.so,libjanus_echotest.so,libjanus_audiobridge.so,libjanus_duktape.so,libjanus_videocall.so,libjanus_textroom.so"
}

# By default, all available transports are enabled and loaded from the transport
# directory at startup (specified via general:transports_folder).
transports: {
	disable = "libjanus_rabbitmq.so,libjanus_pfunix.so,libjanus_nanomsg.so,libjanus_http.so"
}

# By default, all available loggers are enabled and loaded from the loggers
# directory at startup (specified via general:loggers_folder).
loggers: {
	disable = "libjanus_jsonlog.so"
}
